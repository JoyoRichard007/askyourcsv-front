{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "streamlit run app.py --server.port ${PORT} --server.address 0.0.0.0 --server.websocketPingInterval 1 --server.enableWebsocketCompression false --server.enableCORS false --server.enableXsrfProtection false",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}